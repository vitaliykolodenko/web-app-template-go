{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="column">
                <h1>This is the about page</h1>
                <p>This came from the ip: {{index .StringMap "remote_ip"}}</p>
            </div>
        </div>
    </div>
{{end}}